import React from 'react';
import ReactDOM from 'react-dom/client';
import AIPromptChat from './LLM3';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <AIPromptChat/>
  </React.StrictMode>
);
